.DS_Store
.claude/settings.local.json
/.antigravitycli

# Build output of the archived TypeScript implementation. Consumers that still
# pin it build it in place; keep the artifacts out of git status.
node_modules/
dist/

.todo-db/*
!.todo-db/config.json

# Generated skill mirrors. Regenerated locally with `skill-sync apply`.
/.agents/skills/
/.claude/skills/.system/
