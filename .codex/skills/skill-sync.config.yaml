# Generated by skill-sync. Do not edit manually.
# Re-run `skill-sync sync` to regenerate.
code:
  review_checklist: When a TODO says a SQL-translation fix is done because a test
    harness passed, check the helper call site. Confirm the test uses the same
    read= value as the wrapper. One dialect passing does not prove all dialects
    work with SQLGlot.
  verify: npm test
