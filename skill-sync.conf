# skill-sync's own skills. See README.md for the format.
#
# The catalog is a local checkout: skill-sync never fetches. Point `source` at
# wherever https://github.com/joeharris76/skill-sync-skills.git is cloned.

target = .claude/skills
target = .agents/skills

source = ~/Developer/skill-sync-skills
rev    = 6e88497695c51adb7cb4049e177984bc6035e458
dir    = skills
skill  = blog
skill  = bossmode
skill  = code
skill  = docs
# Shared prerequisites. There is no dependency resolver, so bossmode's
# depends: shared-agent-execution is listed explicitly.
skill  = shared-agent-execution
skill  = shared-change-framework
skill  = shared-investigation-framework
skill  = shared-review-protocol
skill  = test
skill  = tidy-perms
skill  = todo

# This repository owns the skill-sync operator skill. Pinned like any other
# source: bump this when skills/skill-sync changes, then re-apply.
source = .
rev    = 4c5abe5c7a96f955b7c04754e9830f157864af7b
dir    = skills
skill  = skill-sync
